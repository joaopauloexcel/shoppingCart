import styled from 'styled-components';

export const Container = styled.div`
    width: 100%;
    height: 90px;
    display: flex;
    background: transparent linear-gradient(95deg, var(---ff0000) 0%, var(---f91e96) 100%) 0% 0% no-repeat padding-box;
    background: transparent linear-gradient(95deg, #FF0000 0%, #F91E96 100%) 0% 0% no-repeat padding-box;
    position: relative;
    z-index: 99;
    top:0px;

    .image {
        padding-top: 24px;
        margin-left: 10%;
    }

`;